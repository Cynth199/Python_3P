#For loop challenge1 (GCSECS.COM)
#Rearrange the two lines of code below
#to display the word "kebab" 10 times
#HINT: There might be some indentation


for i in range(10):
  print("kebab")

#For loop challenge2
#The code below displays the answers to the...
#2 times table. Change it to display the..
#answers to the 3 times table

for i in range(0, 25, 3):
    print(i)
 #third number in range seems to determine multiples

#For loop challenge3 (GCSECS.COM)
#The code below is mixed up
#It should ask the user to enter their name.
#It should then display the name twice
#If possible, upgrade the code to...
#ask the user how many times they want...
#their name to be displayed

name = str(input("Input your name>>>"))
number = int(input("how many times"))
for i in range(number):
  print(name)

#For loop challenge4(GCSECS.COM)
#The code below is mixed up and has 1 error
#It should ask the user to enter a times table
#It should then display the times table
#Also, you may need to fix indentation

number = int(input("Enter a times table: "))
for i in range(1,13):
  print("showing the",number,"times table")
  print(i ,"x", number, "=", i*number)

#For loop challenge5 (GCSECS.COM)
#The code below is mixed up and has an error
#It should countdown from 10 then display "blast off"
#Also, you may need to fix indentation

for i in range(10, 0, -1):
  print (i)
print("!!Blast off!!")

#For loop challenge6 (GCSECS.COM)
#The code below is mixed up and has an error
#It should display the 7 colours of the rainbow...
#...in order and count them too.
#The colours are in a 1D array (or list)
#HINT: You may have to fix indentation too

number = 0
rainbow =["red","orange","yellow","green","blue","indigo", "violet"]
for i in rainbow:
  number = number + 1
  print ("Colour",number,"is", i)

#For loop challenge7 (GCSECS.COM)
#Create some code that..
#asks a user for an int below 100
#displays a backwards count to zero

number = int(input("Number x<100"))
for i in range(number, 0, -1):
  print (i)

#For loop challenge8 (GCSECS.COM)
#Lottery number generator
#use import random on line 1
#use a = random.randint(0,50) to...
#generate a random integer between 0 and 50
#use a for loop to generate...
#6 random integers 

import random
for i in range(0, 6):
  a = random.randint(0,50)
  print(a)

#While loop challenge1 (gcsecs.com)
#Rearrange the four lines of code below
#to get the while loop to work.
#It should keep asking for the password until "kebab" is entered.
#HINT: There might be some indentation

password = ""
while password != "kebab":
  password = input('Please enter a password: ')  
print ("correct password accepted")

#While loop challenge2 (gcsecs.com)
#Rearrange the four lines of code below
#to get the while loop to work.
#It should keep asking "are we there yet" until "y" entered
#You might need to indent some lines

answer = "N"
while answer != "Y":
  print("Are we there yet?")
  answer = str(input("Please respond, y or n: ").upper())
print("Yay! We are there!!")

#While loop challenge3 (gcsecs.com)
#Rearrange the four lines of code below
#to get the while loop to work.
#It should keep asking "Press 1 for choice or 2 to exit"
#until "2" is entered
#You might need to indent some lines

StayInLoop = 1
while StayInLoop != 2:
  print("Press 1 for  choice or 2 to exit")
  StayInLoop = int(input("Please respond, 1 or 2: "))
print("Exited")

#While loop challenge4 (gcsecs.com)
#There is an error in the code below
#It is meant to display Loop 1 to Loop 10 then "End"
#Can you fix it?

loopCount = 1
loopCount = loopCount + 1
loopCount = 1
while loopCount < 11:
  print("Loop", loopCount)
  loopCount = loopCount + 1
print("End")

#While loop challenge5 (gcsecs.com)
#Test this code by running it then hitting the enter button
#The code should stay in a loop until a username is entered
#Add a line of code that displays the username entered and..
#some text that says "accepted" (this message should NOT be repeated)

username = ""
while not username:
  username = str(input("Enter username: "))
  print("username entered")
print("username accepted")

#While loop challenge6 (gcsecs.com)
#There is a line of code missing below
#The point of al the code is to generate a random number, then..
#asks the user to try and guess it
#Can you fix it?
#Look through the code then enter a fix below

import random
number=random.randint(1,10)
userGuess=int(input("Guess the number between 1-10"))


# Add one line of code here to allow the user to
#..guess the random number multiple times
# Hint: You might need to indent some code

t = 1
while userGuess!=number:
  if number < userGuess:
      userGuess=int(input("Guess to High, try again: "))
  else:
      userG8uess=int(input("Guess to Low, try again: "))
print("Yay! You got it correct!!!")

#Relational/Comparison Operator challenge1
#Run the code, it returns "False"
#Change the operator so that...
#.."True" is displayed

print(10 > 5)

#Relational/Comparison Operator challenge2
#Run the code, it returns "False"
#Change the operator so that...
#.."True" is displayed

print(100 <= 100)

#Relational/Comparison Operator challenge3
#Run the code, it returns "False"
#Change the operator so that...
#.."True" is displayed

print(100 == 100)

#Relational/Comparison Operator challenge4
#Run the code, it returns "False"
#Change the operators so that...
#.."True" is displayed


print(100 != 1 and 5 < 10 )


























